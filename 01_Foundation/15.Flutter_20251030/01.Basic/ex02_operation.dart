void main(){
  // 1.산술(arithmetic) 연산자 - +, -, *, /, ~/, %

  // 나누기 연산자(/) -> 결과값은 double 변환되어서 옴
  int num1 = 10;
  int num2 = 5;

  //에러가 발생한다
  // int result = num1/num2;
  double result = num1/num2;

  // 1.1.몫 연산자 (~/)


  // 1.2.논리 연산자
  // ==, !=, >, <, >=, <=  true false 결과 반환

  // 할당 연산자 = 복합 대입 연산자( =, +=, -=, *=, /=, %=, ~/=)
  // num3/= 3 안된다네


  // 3. 논리 연산자 (&&, ||, !)
  // 동일

  // 4. 증감 연산자 (++ , --)
  // 동일


  // 5. 삼항 연산자 (condition ? true : false)




  

}